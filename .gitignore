# 환경변수 (절대 올리면 안됨)
.env

# 패키지 (용량 크고 불필요)
node_modules/

# 로그
*.log
npm-debug.log*

# OS 자동 생성 파일
.DS_Store
Thumbs.db

# 에디터 설정
.vscode/
.idea/

# Firebase
.firebase/
firebase-debug.log
firestore-debug.log
ui-debug.log

# Firebase 서비스 계정 키 (절대 커밋 금지!)
serviceAccountKey.json
*-firebase-adminsdk-*.json

# SQL 덤프 파일 (학생 비밀번호 평문 포함, 절대 커밋 금지!)
db 파일/